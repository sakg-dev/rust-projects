Calculator: Parse user given basic arithmetic operation and calculate

e.g: f("5+7") -> 12

works with max 1 expression(like 5+8 or 22/7), i would be adding for multiple too

for topics: 
    - Enums
    - Match case and if let
    - loops
    - crate(yet to learn)
    - ownership and borrowing
